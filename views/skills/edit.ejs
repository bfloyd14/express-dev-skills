<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dev Skills</title>
</head>
<body>
  <h1>Edit Skill</h1>
  <form action="/skills/<%= skill._id %>?_method=PUT" method="PUT" autocomplete="off">
    <input type="text" name="strength" value="<%= skill.strength %>">
    <input type="checkbox" name="have"<%= skill.have ? 'checked' : '' %>>
    <button type="submit">Save Skills</button>
  </form>
</body>
</html>